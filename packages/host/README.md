# @moor/host

Execution-host commands, sessions, projects, Agent drivers, integrations and durable
transactions. The package owns Host-side authorization rechecks and persistence; it
does not import client, gateway or application source.
