# @moor/client

Typed request construction, Host response validation, original-operation records and
transport adapters. Browser-neutral capabilities live at the package root; filesystem
and local HTTP adapters use explicit `node/` entries. The package does not depend on
Host, gateway or application source.
