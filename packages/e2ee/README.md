# @moor/e2ee

Encryption, trust verification and endpoint identity capabilities. Environment-neutral
cryptography and trust APIs live at the package root; private filesystem and SQLite
locking capabilities are exposed only through explicit `node/` subpaths.
