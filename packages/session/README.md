# @moor/session

Moor session format v1, CRDT and metadata primitives, snapshots, deltas and pure
document projections. This package does not authorize requests, confirm delivery,
persist host data or execute Agents.
