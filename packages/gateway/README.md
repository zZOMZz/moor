# @moor/gateway

Shared Node HTTP/WebSocket ingress, account and product-directory metadata, and
typed target dispatch. Authentication providers and push delivery are injected by
applications. The package stores no session bodies and does not import Host execution.
