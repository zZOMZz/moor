# @moor/protocol

Versioned Moor request, response, event and scope contracts. This package contains
pure schemas, validation and public error semantics. It must not import application,
host, persistence, Electron, React or CRDT implementations.
