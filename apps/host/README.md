# @moor/app-host

Execution-host startup, private configuration and component assembly. Business logic,
Agent drivers and persistence come from `@moor/host`; local HTTP/WS ingress comes
from `@moor/gateway`.
