# @moor/app-web

Web/PWA and the renderer UI shared by desktop. Source is grouped by feature, with
browser and desktop capabilities entering through typed client adapters. Browser
code does not import Host, gateway, Electron or Node implementations.
