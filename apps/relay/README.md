# @moor/app-relay

Self-hosted relay startup, account recovery, Google OIDC and Web Push provider
configuration. Generic ingress and directory behavior is supplied by `@moor/gateway`;
the relay never stores session bodies.
