# @moor/app-cli

CLI argument parsing, command orchestration, private CLI state and terminal output.
Shared request, transport, session and encryption behavior comes from public workspace
packages. Existing `cli` and `security` command entry points remain compatible.
